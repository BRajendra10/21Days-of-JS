// make an object and print it.

var book = {
    name: "crime and punishment",
    author: "fyodor dostoyvsky",
    published: 1866,
    price: 450.00,
};

console.log(`
    book name: ${book.name}
    book's author: ${book.author}
    book published in : ${book.published}
    price of the book: ${book.price}
    
`);